[build-system]
requires = ["setuptools>42.00", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
addopts = "--cov=pykronecker --cov-report=html --cov-config=.coveragerc"
testpaths = [
    "tests",
]

